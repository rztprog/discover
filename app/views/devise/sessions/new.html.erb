<div class="d-flex align-items-center">
  <div class="imageForm col-4">
  </div>
  <div class="col-8">
    <div class="container-sign">
      <div class="d-flex flex-column mb-3">
        <h2>Se connecter</h2>
        <span>Bienvenue sur Discover ! Connectez-vous pour accéder au site web</span>
        <span>Vous avez oublié votre <%= render "devise/shared/links" %></span>
      </div>
      
      <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div>
          <%= f.input :email,
                      required: false,
                      autofocus: true,
                      input_html: { autocomplete: "email" }%>
          <%= f.input :password,
                      label: "Mot de passe",
                      required: false,
                      input_html: { autocomplete: "current-password" } %>
          <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
        </div>

        <div class="form-actions">
          <%= f.button :submit, "Se connecter", class: "btn-signin" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
